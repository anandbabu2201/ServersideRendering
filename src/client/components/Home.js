import React from 'react';

const Home=()=>{
    return (
    <div>
    <div>Iam the very very best home component</div>
    <button onClick={()=>console.log('Hi there')}>press me!</button>
    </div>);
};

export default Home;
